# Machine Learning

This repository includes two machine learning examples using Python code for Linear Regression and Classification.

In each instance the create_model.py code trains and creates a machine learning model. The model is the saved as file. The test_model.py file is then used to test the saved model with unseen data. 

## Packages

You will find the necessary packages you need to install in the requirements.txt file.
